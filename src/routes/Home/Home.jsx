import { useContext } from 'react';
import { CardsContext } from '../../context/CardContext';

const Home = () => {
    return (
        <>
        <h1>Inicio</h1>
        </>
    );
};
export default Home;